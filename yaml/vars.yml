- name: Demo On variables
  hosts: localhost
  vars:
    URL: example.com
  tasks:
    - name: Print hello world
      ansible.builtin.debug:
        msg: Hello World
    - name: Demo on Play Vars
      ansible.builtin.debug:
        msg: "{{ URL }}"